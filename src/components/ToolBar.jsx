import React from 'react';

const ToolBar = ({height}) => {
  const styles = {
    height: height
  };

  return (
    <div 
      className='ToolBar'
      style={styles}
    >
    </div>
  );
}

export default ToolBar;